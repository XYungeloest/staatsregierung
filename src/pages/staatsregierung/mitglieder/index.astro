---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Breadcrumbs from '../../../components/Breadcrumbs.astro';
import GovernmentMemberCard from '../../../components/portal/GovernmentMemberCard.astro';
import GovernmentNavigation from '../../../components/portal/GovernmentNavigation.astro';
import SectionHero from '../../../components/portal/SectionHero.astro';
import { getGovernmentUrl, getHomeUrl } from '../../../lib/portal/index.ts';
import { loadCurrentGovernmentMembers } from '../../../lib/portal/content.ts';

const members = await loadCurrentGovernmentMembers();
const leadership = members.slice(0, 2);
const cabinetMembers = members.filter((member) => member.reihenfolge > 2 && member.slug !== 'emma-mueller');
const chancelleryLeadership = members.filter((member) => member.slug === 'emma-mueller');
---

<BaseLayout title="Regierungsmitglieder" description="Übersicht der Regierungsmitglieder des Ostdeutschen Freistaates.">
  <Breadcrumbs
    items={[
      { label: 'Startseite', href: getHomeUrl() },
      { label: 'Staatsregierung', href: getGovernmentUrl() },
      { label: 'Regierungsmitglieder' },
    ]}
  />

  <SectionHero
    eyebrow="Staatsregierung"
    title="Regierungsmitglieder"
    lead="Ämter, Ressorts und biografische Angaben der Mitglieder des aktuellen Kabinetts."
    variant="government"
    facts={[{ label: 'Mitglieder', value: String(members.length) }]}
  />
  <GovernmentNavigation />

  <section class="section section-muted">
    <div class="section-header">
      <h2>Leitung</h2>
      <p>
        Die politische Führung der Staatsregierung liegt beim Ministerpräsidenten und seiner
        Stellvertretung.
      </p>
    </div>
    <div class="spotlight-grid">
      {leadership.map((member) => (
        <GovernmentMemberCard member={member} />
      ))}
    </div>
  </section>

  <section class="section">
    <div class="section-header">
      <h2>Mitglieder des Kabinetts</h2>
      <p>Alle weiteren Regierungsmitglieder sind mit ihren jeweiligen Zuständigkeiten erschlossen.</p>
    </div>
    <div class="card-grid card-grid--two">
      {cabinetMembers.map((member) => (
        <GovernmentMemberCard member={member} variant="compact" />
      ))}
    </div>
  </section>

  <section class="section section-muted">
    <div class="section-header">
      <h2>Staatskanzlei</h2>
      <p>Die Leitung der Staatskanzlei gehört zur Staatsregierung, wird aber nicht als Staatsministerium gezählt.</p>
    </div>
    <div class="card-grid card-grid--two">
      {chancelleryLeadership.map((member) => (
        <GovernmentMemberCard member={member} variant="compact" />
      ))}
    </div>
  </section>
</BaseLayout>
