---
import { formatTopicStatus, getTopicStatusTone } from '@ostrecht/shared/lib/portal/presentation.ts';
import type { Themenstatus } from '@ostrecht/shared/lib/portal/index.ts';

interface Props {
  status: Themenstatus;
}

const { status } = Astro.props as Props;
const tone = getTopicStatusTone(status);
---

<span class:list={['status-badge', `status-badge--${tone}`]}>{formatTopicStatus(status)}</span>
