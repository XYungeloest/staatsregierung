name: OstRecht-Vollbestand-Smoke und Korpus-Audit

# Wöchentlich und manuell, unabhängig vom Änderungsscope: (1) corpus_audit – die vollständigen
# Content-Audits über den gesamten Rechtsbestand (content:check: Import-Audit, Konsolidierung,
# Schemas, Metadaten, Ableitungen, Reststellen) und die Korpus-Tests (tests/corpus/:
# Projektionsnachweis, Seed, Referenzindex); (2) full_corpus_smoke – Verifikation, Browser- und
# Barrierefreiheits-Smoke gegen den gebauten OstRecht-Worker mit der vollständigen lokalen
# D1-Projektion (alle Normen). Pull Requests laufen dieselben Audits nur bei Inhalts- und
# Pipeline-Änderungen und die Smokes gegen das Testfixture (docs/DEPLOYMENT_RUNBOOK.md,
# Testkategorien). Der Seed kommt aus dem Actions-Cache (Fingerabdruck aus
# Projektionslogik, Bestand, Stichtag, Seed-Werkzeugen und Wrangler-Version); nur ohne passenden
# Eintrag wird projiziert. `refresh_seed` erzwingt die Projektion und beweist damit den
# Cache-Miss-Pfad. Es wird keine Cloudflare-Datenbank berührt.

on:
  workflow_dispatch:
    inputs:
      refresh_seed:
        description: Seed neu projizieren statt aus dem Cache (Nachweis des Cache-Miss-Pfads)
        required: false
        default: false
        type: boolean
  schedule:
    - cron: '30 3 * * 1'

concurrency:
  group: full-corpus-smoke
  cancel-in-progress: true

jobs:
  corpus_audit:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Repository auschecken
        uses: actions/checkout@v7
      - name: Node.js und Abhängigkeiten
        uses: ./.github/actions/setup-node-modules
      - name: Dokumentation und vollständige Content-Audits über den gesamten Rechtsbestand
        run: |
          START=$(date +%s)
          node scripts/check-docs.mjs
          npm run content:check
          echo "content=$(( $(date +%s) - START ))" >> "$RUNNER_TEMP/timings.txt"
      - name: Korpus-Tests (Projektionsnachweis auf dem Fixture, Seed, Referenzindex)
        run: |
          START=$(date +%s)
          npm run test:corpus
          echo "corpus=$(( $(date +%s) - START ))" >> "$RUNNER_TEMP/timings.txt"
      - name: Laufzeiten festhalten
        if: ${{ always() }}
        run: |
          {
            echo '### Laufzeiten corpus_audit'
            echo
            echo '| Schritt | Sekunden |'
            echo '| --- | --- |'
            if [ -f "$RUNNER_TEMP/timings.txt" ]; then
              sed -E 's/^([^=]+)=(.*)$/| \1 | \2 |/' "$RUNNER_TEMP/timings.txt"
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  full_corpus_smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Repository auschecken
        uses: actions/checkout@v7
      - name: Node.js und Abhängigkeiten
        uses: ./.github/actions/setup-node-modules
      - name: OstRecht bauen
        run: |
          START=$(date +%s)
          npm run build:recht
          echo "build=$(( $(date +%s) - START ))" >> "$RUNNER_TEMP/timings.txt"
      - name: Chromium installieren
        run: npx playwright install --with-deps chromium
      - name: Vollständige lokale D1-Projektion bereitstellen (Cache oder Projektion)
        id: seed
        uses: ./.github/actions/d1-seed
        with:
          cache: 'true'
          force: ${{ inputs.refresh_seed == true && 'true' || 'false' }}
      - name: Lokale D1-Projektion gegen den Git-Bestand verifizieren
        run: |
          START=$(date +%s)
          npm run norms:runtime:d1-verify -- --local --fts-integrity
          echo "verify=$(( $(date +%s) - START ))" >> "$RUNNER_TEMP/timings.txt"
      - name: Zentrale Seiten barrierefrei prüfen
        env:
          SITE_TARGETS: law
        run: |
          START=$(date +%s)
          npm run test:a11y:ci
          echo "a11y=$(( $(date +%s) - START ))" >> "$RUNNER_TEMP/timings.txt"
      - name: Zentrale Nutzerwege in Chromium prüfen
        env:
          SITE_TARGETS: law
        run: |
          START=$(date +%s)
          npm run test:browsers:ci
          echo "browser=$(( $(date +%s) - START ))" >> "$RUNNER_TEMP/timings.txt"
      - name: Laufzeiten festhalten
        if: ${{ always() }}
        env:
          SEED_STATUS: ${{ steps.seed.outputs.status }}
          SEED_BUILD: ${{ steps.seed.outputs.build-seconds }}
          SEED_VERIFY: ${{ steps.seed.outputs.verify-seconds }}
          SEED_INSTALL: ${{ steps.seed.outputs.install-seconds }}
        run: |
          {
            echo '### Laufzeiten full_corpus_smoke'
            echo
            echo '| Schritt | Wert |'
            echo '| --- | --- |'
            echo "| Seed | ${SEED_STATUS:-unbekannt} (Projektion ${SEED_BUILD:-0} s, Verifikation ${SEED_VERIFY:-0} s, Einsetzen ${SEED_INSTALL:-0} s) |"
            if [ -f "$RUNNER_TEMP/timings.txt" ]; then
              sed -E 's/^([^=]+)=(.*)$/| \1 | \2 s |/' "$RUNNER_TEMP/timings.txt"
            fi
          } >> "$GITHUB_STEP_SUMMARY"
          echo "Laufzeiten: seed=${SEED_STATUS:-unbekannt}/${SEED_BUILD:-0}s $( [ -f "$RUNNER_TEMP/timings.txt" ] && paste -sd ',' "$RUNNER_TEMP/timings.txt" )"
